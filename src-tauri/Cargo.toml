[package]
name = "deskpet"
version = "0.1.0"
description = "A lightweight desktop pet built with Tauri and PixiJS"
authors = ["DeskPet"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
